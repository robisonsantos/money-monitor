# Money Monitor - Environment Configuration Template
# Copy this file to .env and customize for your local development

# ===== DATABASE CONFIGURATION =====
# PostgreSQL Database Configuration (Docker)
DATABASE_URL=postgresql://postgres:dev_password_123@localhost:5432/money_monitor
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=dev_password_123
DB_NAME=money_monitor
DB_SSL=false

# ===== SECURITY CONFIGURATION =====
# Encryption key for financial data (REQUIRED)
# This is a development-only key - NEVER use in production
# Generate a secure key for production: openssl rand -hex 32
ENCRYPTION_KEY=dev_key_32_bytes_for_local_development_only_never_use_in_production_123456

# ===== APPLICATION CONFIGURATION =====
# Node environment
NODE_ENV=development

# ===== DOCKER CONFIGURATION =====
# PostgreSQL data directory - customize this path for your system
# Choose ONE of the options below or specify your own path

# Option 1: macOS - User Data directory (recommended for Mac users)
DATA_DIR=$HOME/Data/postgres_dkr

# Option 2: Linux - User docker data directory (recommended for Linux users)
# DATA_DIR=$HOME/docker-data/postgres_dkr

# Option 3: Project relative directory (works on all systems, easier to clean up)
# DATA_DIR=./postgres_data

# Option 4: Custom absolute path (replace with your preferred location)
# DATA_DIR=/path/to/your/custom/postgres/data

# Option 5: Windows users (if using WSL or Git Bash)
# DATA_DIR=/c/Users/$USER/docker-data/postgres_dkr

# ===== DOCKER POSTGRES CONFIGURATION =====
# These variables are used by docker-compose.yml
POSTGRES_DB=money_monitor
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev_password_123

# ===== PGADMIN CONFIGURATION (OPTIONAL) =====
# pgAdmin web interface configuration
PGADMIN_DEFAULT_EMAIL=admin@moneymonitor.local
PGADMIN_DEFAULT_PASSWORD=admin123

# ===== DEVELOPMENT NOTES =====
# 1. Uncomment your preferred DATA_DIR option above
# 2. The encryption key above is for development only
# 3. Generate a secure key for production: openssl rand -hex 32
# 4. Never commit your actual .env file to version control
# 5. Restart Docker containers after changing DATA_DIR: npm run db:stop && npm run db:start
# 6. Default DATA_DIR if none specified: ./postgres_data (project directory)

# ===== QUICK SETUP EXAMPLES =====
# macOS users:     cp .env.example .env (then uncomment macOS DATA_DIR)
# Linux users:     cp .env.example .env (then uncomment Linux DATA_DIR)
# Windows users:   cp .env.example .env (then uncomment Windows DATA_DIR)
# Simple setup:    cp .env.example .env (then uncomment project relative DATA_DIR)
